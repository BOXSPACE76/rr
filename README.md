# 1. Add all files
git add .

# 2. Commit
git commit -m "Initial commit with Cursor configuration"

# 3. Rename branch to main
git branch -M main

# 4. Link to your remote repo (Replace URL with your actual repo URL)
git remote add origin https://github.com/YOUR_USERNAME/my-cursor-project.git

# 5. Push
git push -u origin main
